#ifndef __TYPES__
#define __TYPES__

enum FractalType {
        Test,
        Mandelbulb,
};

enum BackgroundType {
        Solid,
        Skybox,
        SkyboxHDR,
};

enum SkyboxTexture {
        Orbital,
        Night,
};

enum SkyboxTextureHDR {
        WinterForest,
        Milkyway,
};

#endif